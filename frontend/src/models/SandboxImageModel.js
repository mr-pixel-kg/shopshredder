export class SandboxImageModel {
    constructor(title, thumbnailUrl, imageName, description, infoLink) {
        this.title = title;
        this.thumbnailUrl = thumbnailUrl;
        this.imageName = imageName;
        this.description = description;
        this.infoLink = infoLink;
    }
}